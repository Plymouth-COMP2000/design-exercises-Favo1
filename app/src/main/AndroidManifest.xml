<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.RestaurantApp"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="24">

        <activity
            android:name="com.restaurant.app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.restaurant.app.LoginActivity" />
        <activity android:name="com.restaurant.app.DashboardActivity" />
        <activity android:name="com.restaurant.app.RegisterActivity" />
        <activity android:name="com.restaurant.app.AddEditMenuItemActivity" />
        <activity android:name="com.restaurant.app.AddEditReservationActivity" />
        <activity android:name="com.restaurant.app.SettingsActivity" />
        <activity android:name="com.restaurant.app.StatisticsActivity" />

    </application>

</manifest>